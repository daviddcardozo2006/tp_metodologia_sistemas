# git switch

Comando moderno para cambiar de rama, introducido en Git 2.23 como alternativa más clara a `git checkout`.

## Cambiar de rama

```bash
git switch nombre-de-la-rama
```

## Crear una rama y moverse a ella

```bash
git switch -c nombre-de-la-rama
```